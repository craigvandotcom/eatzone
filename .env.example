# OpenRouter API for AI features
OPENROUTER_API_KEY=""

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_SECRET_KEY=""

# Upstash Redis for rate limiting (existing)
KV_URL=""
KV_REST_API_READ_ONLY_TOKEN=""
REDIS_URL=""
KV_REST_API_TOKEN=""
KV_REST_API_URL=""

# Test Credentials (for Playwright E2E tests)
TEST_USER_EMAIL=""
TEST_USER_PASSWORD=""

# Test Mock Emails (for Jest unit/component tests)
TEST_MOCK_EMAIL=""
TEST_INVALID_EMAIL=""

# Application Configuration (Optional - uses defaults if not set)
# Image processing limits
MAX_IMAGE_FILE_SIZE="10485760"  # 10MB in bytes
MAX_IMAGES_PER_REQUEST="5"      # Maximum images per API request
MAX_CAMERA_IMAGES="3"           # Maximum images in camera capture

# Background processing configuration
BACKGROUND_BATCH_SIZE="10"            # Number of items to process per batch
BACKGROUND_RETRY_DELAY_MS="5000"      # Base delay between retries (5 seconds)
BACKGROUND_PROCESS_INTERVAL_MS="60000" # Interval between background runs (1 minute)
MAX_RETRY_ATTEMPTS="3"                # Maximum retry attempts for failed operations

# Database migration settings
MIGRATION_BATCH_SIZE="100"      # Records to migrate per batch
MIGRATION_DELAY_MS="500"        # Delay between migration batches

# Rate limiting configuration
IMAGE_ANALYSIS_RATE_LIMIT="10"  # Requests per minute for image analysis